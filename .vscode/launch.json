{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Go debugger",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/src",
            "args": [
                "-o", "output.log"
            ],
            "envFile": "${workspaceFolder}/.env",
        }
    ]
}